spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    properties.hibernate.temp.use_jdbc_metadata_defaults: false

  datasource:
      platform: postgres
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/turniere
      username: postgres
      password: password

  thymeleaf:
      cache: false
